services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "5099:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: Server=sqlserver,1433;Database=DailyRentalHomes;User Id=sa;Password=Your_strong_password123;TrustServerCertificate=True
      Token__Issuer: DailyRentalHomes
      Token__Audience: DailyRentalHomesClients
      Token__Key: LOCAL_DEVELOPMENT_KEY_CHANGE_LATER_123456789
      Token__Minutes: 1440
      Notifications__WorkerEnabled: "false"
    depends_on:
      sqlserver:
        condition: service_started

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: Your_strong_password123
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql

volumes:
  sqlserver-data:
